""" if one element without comma that will be integer type.
    Otherwise, it's tuple and no element in parentheses it's also tuple
"""
abc = (2,)
efg = 1,

print(type(abc))
print(type(efg))  # No Parenthesis and an element after a comma it's also a tuple.

num_tuple = (56, 76, 73, 25, 64, 26, 74, 55, 85, 16)
print(num_tuple[3])

# num_tuple[4]=100

