str1 = "Hello"
str2 = "World"

# You can use the + operator to concatenate
text = str1 + " " + str2  # # Adds a space between "Hello" and "World"
# text = " ".join([str1, str2])
print(text)

# You can repeat a string multiple times using the * operator.
text = "Hello"
word = text * 3
# word = ", ".join(text * 1 for i in range(3))
print(word)
