word = "python"
print(len(word))  # To finding the length

print(word.upper())  # Converts all characters to uppercase.
print(word.lower())  # Converts all characters to lowercase.

words = "   Hello   "
print(words.strip())  # Removes leading and trailing whitespace (or specified characters).

print(word.replace("th", "LL"))  # Replaces occurrences of a substring with another substring.

text = "Hello world"
print(text.split())
user_input = input("Entr anything: ").split()  # Splits a string into a list based on a delimiter.

print(", ".join(user_input))  # Joins elements of an iterable into a single string.


