"""
5. Given a list of names, concatenate them into a single string separated by spaces'
"""

name_list = ["Raju", "Suresh", "Murali", "Unni", "Alex"]

names = " ".join(name_list)  # concatenate using join() function

print(names)
print(type(names))  # Checking the type to confirm its string.
